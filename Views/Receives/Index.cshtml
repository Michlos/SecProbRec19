@model IEnumerable<SecProbRec19.Models.Receive>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
@{ 
    Receive autoModel = new Receive();
    autoModel.MailId = 55;
} 
<p>
    <a asp-action="Create">Create New</a>
    @*levando o id na unha pra teste*@|
    @*<a asp-action="AutoCreate" asp-route-id="@autoModel.MailId">AutoCreate</a>*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MailId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IpAddress)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MailId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IpAddress)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
